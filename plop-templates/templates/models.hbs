import services from "./services";

export default {
    state: {
        //列表页面Id
        listPageId: `div{{name}}ListPage_${new Date().getTime()}`,
        //表单页面Id
        formPageId: `div{{name}}FormPage_${new Date().getTime()}`,
    },

    reducers: {
        updateDatas(state, { datas }) {
            return { ...state, ...datas };
        }
    },

    effects: {}
};
